<Page
    x:Class="Exam_uwp.view.ShowFile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Exam_uwp.view"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

        </Grid>
        <StackPanel x:Name="ParentStackPanel" Width="293">
            <RelativePanel>
                <TextBox x:Name="Name" Margin="0, 30, 0, 0" Header="Current Files" HorizontalAlignment="Left" Text="" VerticalAlignment="Top" Width="293"/>
                <TextBlock x:Name="name" RelativePanel.Below="Name"/>
            </RelativePanel>
            <RelativePanel>
                <TextBox x:Name="Content" Margin="0, 30, 0, 0" Header="File Content" HorizontalAlignment="Stretch" VerticalAlignment="Top" Width="293" Height="200"/>
                <!--<TextBlock x:Name="content" RelativePanel.Below="Content"/>-->
            </RelativePanel>
        </StackPanel>
    </Grid>
</Page>
